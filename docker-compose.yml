version: '3.9'

services:
  postgres:
    image: postgres:latest
    ports:
      - 5433:5432
    volumes:
      - ~/apps/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=S3cret
      - POSTGRES_USER=glady
      - POSTGRES_DB=gladytt